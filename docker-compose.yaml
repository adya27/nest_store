version: '3.8'
services:
  top-api:
    image: top-api:latest
    container_name: top-api
    restart: always
    ports:
      - 3000:3000
    volumes:
      - type: bind
        source: ${PWD}/.env
        target: /opt/app/.env